import type { NextConfig } from "next";

// Sem configuração especial: o padrão do Next atende o projeto. A opção
// experimental que existia aqui só repetia o padrão e imprimia um aviso de
// depreciação em todo build, o que assusta à toa em log de deploy.
const nextConfig: NextConfig = {};

export default nextConfig;
